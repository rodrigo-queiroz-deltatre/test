name: Test
on:
  workflow_call
jobs:
  bash-example:
    runs-on: ubuntu-latest
    steps:
      - name: Reuse output
        id: getPath
        run: |
            path=something
            echo "path=path" >> $GITHUB_OUTPUT
      - name: Print path
        run: echo ${{ steps.getPath.outputs.path }}
